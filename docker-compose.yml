services:
  # MongoDB сервис закомментирован - используется внешний MongoDB
  # mongodb:
  #   image: mongo:7-jammy
  #   container_name: kvansum-mongodb
  #   restart: unless-stopped
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: kvansum
  #     MONGO_INITDB_ROOT_PASSWORD: kvansum_dev_password
  #     MONGO_INITDB_DATABASE: kvansum
  #   ports:
  #     - '27017:27017'
  #   volumes:
  #     - mongodb_data:/data/db
  #     - mongodb_config:/data/configdb
  #   healthcheck:
  #     test: ['CMD', 'mongosh', '--eval', 'db.adminCommand("ping")']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kvansum-api
    restart: unless-stopped
    ports:
      - '${PORT:-3001}:${PORT:-3001}'
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: ${PORT:-3001}
      MONGODB_URI: ${MONGODB_URI}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      CORS_ORIGINS: ${CORS_ORIGINS}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_TIME: ${JWT_EXPIRATION_TIME}
    # depends_on убран - используется внешний MongoDB
    volumes:
      - ./src:/app/src
    networks:
      - mongo-network

# Volumes закомментированы - используется внешний MongoDB
# volumes:
#   mongodb_data:
#   mongodb_config:

networks:
  mongo-network:
    external: true
    name: mongo_mongo-network

