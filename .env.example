# Application
NODE_ENV=development
PORT=3001

# MongoDB
# Local development (without Docker)
MONGODB_URI=mongodb://localhost:27017/kvansum
# Local development (with Docker Compose - use when running inside container)
# MONGODB_URI=mongodb://kvansum:kvansum_dev_password@mongodb:27017/kvansum?authSource=admin
# Local development (with Docker Compose - use when running on host)
# MONGODB_URI=mongodb://kvansum:kvansum_dev_password@localhost:27017/kvansum?authSource=admin
# Production (MongoDB Atlas)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Database name (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ, РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ Р±СѓРґРµС‚ РёСЃРїРѕР»СЊР·РѕРІР°С‚СЊСЃСЏ РёРјСЏ РёР· URI РёР»Рё "kvansum")
DB_NAME=kvansum

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# JWT (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ, РґР»СЏ Р±СѓРґСѓС‰РµР№ СЂРµР°Р»РёР·Р°С†РёРё)
# JWT_SECRET=your-secret-key-here
# JWT_EXPIRES_IN=7d

# Telegram Bot (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ, РґР»СЏ Р±СѓРґСѓС‰РµР№ СЂРµР°Р»РёР·Р°С†РёРё)
# TELEGRAM_BOT_TOKEN=your-bot-token-here
# TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram/webhook